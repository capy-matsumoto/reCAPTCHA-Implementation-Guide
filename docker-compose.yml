version: '3'
services:
  recaptcha:
    build:
      context: .
    ports:
      - "8880:80"
    volumes:
      - ./src:/var/www/html
    environment:
      - FIDO_ID=${FIDO_ID}
      - BEARER_TOKEN=${BEARER_TOKEN}
